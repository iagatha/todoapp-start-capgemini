package com.mycompany.todoapp;

import com.mycompany.todoapp.controller.ProjectController;
import com.mycompany.todoapp.controller.TaskController;
import com.mycompany.todoapp.model.Projects;
import com.mycompany.todoapp.model.Tasks;
import com.mycompany.todoapp.util.ConnectionFactory;
import java.sql.Connection;
import java.util.Date;
import java.util.List;

public class TodoApp {

    public static void main(String[] args) {
        
       // ProjectController projectController = new ProjectController();
        
        //Projects project = new Projects();
        //project.setName("Projeto teste");
        //project.setDescription("descrição");
        //projectController.save(project);
        
        
        //ProjectController projectController = new ProjectController();
       // Projects project = new Projects();
       // project.setId(1);
        //project.setName("Novo nome do projeto");
        //project.setDescription("descrição");
        //projectController.update(project);
       
        //List<Projects> projects = projectController.getAll();
       //System.out.println("Total de projetos = " + projects.size());
       
      // projectController.removeById(1);
       
         // TaskController taskController = new TaskController();
       
        // Tasks task = new Tasks();
         //task.setIdProjects(1);
         //task.setName("1 tarefa");
         //task.setDescription("tem uma decrição");
         //task.setNotes("sem notas");
         //task.setDeadline(new Date());
         //task.setCompleted(false);
         
         //taskController.save(task);
         
         
         //task.setName("Alterar telas da aplicação");
           //task.setIdProjects(1);
         //task.setDescription("nnova decrição");
         //task.setNotes("nova notas");
         //task.setDeadline(new Date());
         
         //taskController.update(task);
         
    }
}
